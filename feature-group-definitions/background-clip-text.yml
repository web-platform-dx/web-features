name: "background-clip: text"
description: "The `background-clip: text` CSS property value allows the background to be clipped to the text in the element."
spec: https://drafts.csswg.org/css-backgrounds-4/#background-clip
compat_features:
  - css.properties.background-clip.text
