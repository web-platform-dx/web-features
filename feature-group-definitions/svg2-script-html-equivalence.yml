name: SVG `<script>` async and defer
description: The `async` and `defer` attributes of the `<script>` SVG element control the loading and execution of scripts in SVG documents.
spec: https://svgwg.org/svg2-draft/interact.html#ScriptElement
