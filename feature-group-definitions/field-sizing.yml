name: field-sizing
spec: https://drafts.csswg.org/css-ui-4/#field-sizing
description: The `field-sizing` CSS property sets whether an element may expand based on its content. The property applies only to elements with a default preferred size, such as a `<textarea>`, which has a fixed size regardless of its content.
