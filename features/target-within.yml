name: :target-within
description: "The `:target-within` CSS pseudo-class represents the target element with an id matching the URL fragment."
spec: https://drafts.csswg.org/selectors-4/#the-target-within-pseudo
group: selectors
compat_features:
  - css.selectors.target-within
