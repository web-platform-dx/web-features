name: String normalize()
description: The `normalize()` method of strings returns the Unicode Normalization Form of the string as a new string.
spec: https://tc39.es/ecma262/multipage/text-processing.html#sec-string-objects
group: string
compat_features:
  - javascript.builtins.String.normalize
