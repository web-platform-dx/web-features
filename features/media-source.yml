name: Media source
description: The `MediaSource` API is a custom data source for media elements commonly used for adaptive streaming. Also known as Media Source Extensions (MSE).
spec: https://w3c.github.io/media-source/
caniuse: mediasource
# This are the main interfaces of MSE. TODO: flesh this out to cover the core
# feature and define new features for significant later additions.
compat_features:
  - api.MediaSource
  - api.SourceBuffer
  - api.SourceBufferList
