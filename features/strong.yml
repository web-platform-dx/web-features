name: <strong>
description: The `<strong>` HTML element brings attention to the content, rendering text as bold by default.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-strong-element
group: html-elements
compat_features:
  - html.elements.strong
