name: '<link rel="expect">'
description: The `rel="expect"` attribute for the `<link>` HTML element hints to browsers to block rendering until the element that it references is connected to the document and fully parsed.
spec: https://html.spec.whatwg.org/multipage/links.html#link-type-expect
compat_features:
  - html.elements.link.rel.expect
