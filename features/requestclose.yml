name: dialog.requestClose()
description: The `requestClose()` method of a `<dialog>` HTML element closes the dialog, firing a `close` event first, which listeners can use to prevent the dialog from closing. This differs from the `close()` method, which closes the dialog immediately.
spec: https://html.spec.whatwg.org/multipage/interactive-elements.html#dom-dialog-requestclose
group: html-elements
compat_features:
  - api.HTMLDialogElement.requestClose
