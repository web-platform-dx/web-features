name: Local network access
description: Restricts the ability to send requests to servers on a user's local network and requiring the user grant the site permission before such requests can be made.
spec: https://wicg.github.io/local-network-access/
compat_features:
  - api.Request.targetAddressSpace
