name: TrackEvent
description: The TrackEvent interface of the HTML DOM API is used for events which represent changes to a set of available tracks on an HTML media element; these events are addtrack and removetrack.
spec: https://html.spec.whatwg.org/multipage/media.html#the-trackevent-interface
#caniuse:
group: html-api
#status:
#  compute_from:
compat_features:
  - api.TrackEvent
  - api.TrackEvent.TrackEvent
  - api.TrackEvent.track