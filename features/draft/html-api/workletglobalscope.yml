name: WorkletGlobalScope
description: The WorkletGlobalScope interface is an abstract class that specific worklet scope classes inherit from. Each WorkletGlobalScope defines a new global environment.
spec: https://html.spec.whatwg.org/multipage/worklets.html#workletglobalscope
#caniuse:
group: html-api
#status:
#  compute_from:
compat_features:
  - api.WorkletGlobalScope