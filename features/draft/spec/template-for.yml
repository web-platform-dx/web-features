draft_date: 2026-08-11
name: <template for>
description: The `<template for="…">` HTML element replaces the content at a named range or insertion point, marked by the processing instructions `<?start name="…">` and `<?end>` or `<?marker name="…">`. You can use this to stream partial updates into the document out of order.
# TODO: Uncomment when https://github.com/whatwg/html/pull/11818 lands
#spec: https://html.spec.whatwg.org/multipage/scripting.html#attr-template-for
# TODO: Uncomment when XXX lands
#compat_features:
#- html.elements.template.for
