name: scrollIntoView()
description: The `scrollIntoView()` method scrolls an element's ancestor containers such that the element is visible to the user.
spec: https://drafts.csswg.org/cssom-view-1/#dom-element-scrollintoview
caniuse: scrollintoview
group: scrolling
status:
  compute_from: api.Element.scrollIntoView
compat_features:
  - api.Element.scrollIntoView
  - api.Element.scrollIntoView.options_parameter
  # TODO: decide whether to push out the baseline high date by including the
  # block and inline parameters. They are also baseline high by now, but were
  # added quite a bit later than scrollIntoView itself.
  - api.Element.scrollIntoView.options_parameter.block_option
  - api.Element.scrollIntoView.options_parameter.inline_option
  # TODO: decide whether to move this option to a separate feature. It's only
  # in Chromium 140+.
  - api.Element.scrollIntoView.options_parameter.container_option
