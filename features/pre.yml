name: <pre>
description: The `<pre>` element epresents preformatted text which is to be presented exactly as written in the HTML file.
spec: https://html.spec.whatwg.org/multipage/grouping-content.html#the-pre-element
group: html
compat_features:
  - api.HTMLPreElement
  - html.elements.pre
