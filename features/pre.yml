name: <pre>
description: The `<pre>` element represents a block element of preformatted text which is presented exactly as written, including all white space.
spec: https://html.spec.whatwg.org/multipage/grouping-content.html#the-pre-element
group: html
compat_features:
  - api.HTMLPreElement
  - html.elements.pre
