name: RegExp.escape
description: The `RegExp.escape()` method replaces characters that are potentially special characters in a regular expression with equivalent escape sequences.
spec: https://tc39.es/proposal-regex-escaping/
compat_features:
  - javascript.builtins.RegExp.escape
