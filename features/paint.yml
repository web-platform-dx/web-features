name: paint()
description: The `paint()` CSS function paints an element's background or border using a custom paint worklet.
spec: https://drafts.css-houdini.org/css-paint-api/
caniuse: css-paint-api
compat_features:
  - css.types.image.paint
  - api.CSS.paintWorklet_static
  - api.PaintWorkletGlobalScope
group: css
