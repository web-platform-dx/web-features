name: "@function"
description: "The `@function` at-rule takes arguments, such as CSS values or custom properties, and returns a CSS value, possibly based on conditional logic such as by using the `@media` at-rule."
spec: https://drafts.csswg.org/css-mixins-1/#function-rule
group: css
# No compat keys yet, this feature is planned to ship with Chromium 139