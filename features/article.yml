name: <article>
description: The `<article>` element is a self-contained composition in a document, page, application, or site, which is intended to be independently distributable or reusable.
spec: https://html.spec.whatwg.org/multipage/sections.html#the-article-element
group: html-elements
compat_features:
  - html.elements.article
