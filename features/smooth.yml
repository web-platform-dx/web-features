name: smooth
description: "The `image-rendering: smooth` CSS value scales images while retaining a gradual color blending that is useful for photos."
spec: https://drafts.csswg.org/css-images-3/#the-image-rendering
group: image-rendering
compat_features:
  - css.properties.image-rendering.smooth
