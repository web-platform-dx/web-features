name: <samp>
description: The `<samp>` element is used to enclose inline text which represents sample (or quoted) output from a computer program.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-samp-element
group: html
compat_features:
  - html.elements.samp
