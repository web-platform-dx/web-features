name: window.print()
description: The `window.print()` method opens the browser's print dialog, to give the user the choice to make a physical copy of the page.
group: printing
spec: https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#printing
compat_features:
  - api.Window.print
