name: ::before and ::after
description: The `::before` and `::after` CSS pseudo-elements set styles of the first and last children of an element. They often are used to add 4
spec: https://drafts.csswg.org/css-pseudo-4/#generated-content
caniuse: css-gencontent
status:
  compute_from:
    - css.selectors.before
    - css.selectors.after
compat_features:
  - css.selectors.before
  - css.selectors.before.animation_and_transition_support
  - css.selectors.after
  - css.selectors.after.animation_and_transition_support
