name: ::before and ::after
description: The `::before` and `::after` CSS pseudo-elements selects the first and last children of an element for styling, and are often used with the `content` property to add generated cosmetic content.
spec: https://drafts.csswg.org/css-pseudo-4/#generated-content
caniuse: css-gencontent
status:
  compute_from:
    - css.selectors.before
    - css.selectors.after
compat_features:
  - css.selectors.before
  - css.selectors.before.animation_and_transition_support
  - css.selectors.after
  - css.selectors.after.animation_and_transition_support
