name: <b>
description: The `<b>` element makes text bold in order to draw the reader's attention to it.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-b-element
group: html-elements
compat_features:
  - html.elements.b
