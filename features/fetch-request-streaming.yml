name: Fetch request streams
description: Setting the `body` property of a request to a `ReadableStream`, when using the `fetch()` method, allows you to stream data to a server.
spec: https://fetch.spec.whatwg.org/#concept-body-stream
compat_features:
  - api.Request.body
