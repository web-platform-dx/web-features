name: ToggleEvent source
description: The `source` attribute of `ToggleEvents` contains the element which triggered the `ToggleEvent` to be fired for a popover, `<dialog>`, or `<details>` element, if applicable.
spec: https://html.spec.whatwg.org/multipage/interaction.html#dom-toggleevent-source
group: html
compat_features:
  - api.ToggleEvent.source
