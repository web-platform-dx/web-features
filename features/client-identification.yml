name: Client identification (user agent)
description: "The `navigator.userAgent` read-only property returns the user agent string for the current browser."
spec: https://html.spec.whatwg.org/multipage/system-state.html#the-navigator-object
compat_features:
  - api.Navigator.userAgent
  - api.WorkerNavigator.userAgent
