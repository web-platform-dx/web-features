name: Title global attribute
description: The `title` global attribute represents the title of an element, with the attribute value typically displayed in a 'tooltip' popup when the mouse is over the node.
spec: https://html.spec.whatwg.org/multipage/dom.html#dom-title
compat_features:
  - api.HTMLElement.title
