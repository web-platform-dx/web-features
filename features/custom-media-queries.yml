name: Custom media queries
description: Named aliases for longer and more complex media queries, deffined once in a `@custom-media` rule and used as reusable variables in multiple `@media` rules.
spec: https://www.w3.org/TR/mediaqueries-5/#custom-mq
group: media-queries
