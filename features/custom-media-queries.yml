name: Custom media queries
description: Named aliases for longer and more complex media queries, deffined once in a `@custom-media` rule and used in multiple times `@media` rules.
spec: https://www.w3.org/TR/mediaqueries-5/#custom-mq
group: media-queries
