name: Grid animation
description: Grid animation allows you to animate the `grid-template-columns` and `grid-template-rows` CSS properties.
spec: https://drafts.csswg.org/css-grid-2/#track-sizing
caniuse: css-grid-animation
group: grid
