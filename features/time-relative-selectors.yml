name: Time-dimensional pseudo-selectors
description: The `:future` and `:past` CSS pseudo-selectors style upcoming or prior elements during media playback.
spec: https://drafts.csswg.org/selectors-4/#time-pseudos
group: selectors
compat_features:
  - css.selectors.future
  - css.selectors.past
  # Note: `:current` has no support anywhere yet.
