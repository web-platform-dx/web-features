name: Absolute positioning
description: "The `position: absolute` CSS declaration removes an element from the normal flow and positions an element relative to its containing block or closest positioned ancestor."
spec: https://drafts.csswg.org/css-position-3/#abspos-insets
group: layout
compat_features:
  - css.properties.position.absolute
