name: <span>
description: The `<span>` HTML element is the generic inline container for content.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-span-element
group: html-elements
compat_features:
  core:
    - html.elements.span # former compute_from
  modifier:
    - api.HTMLSpanElement
