name: symbols()
description: The `symbols()` CSS function defines counter styles directly as a value of properties such as `list-style`. This provides a simple alternative to the `@counter-style` CSS at-rule.
spec: https://drafts.csswg.org/css-counter-styles-3/#symbols-function
group: lists
compat_features:
  - css.properties.list-style-type.symbols
  - css.properties.list-style.symbols
