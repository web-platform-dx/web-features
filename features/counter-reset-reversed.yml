name: Reversed counter-reset
description: "The `reversed()` CSS function is used with `counter-reset` to create a counter that counts down."
spec: https://drafts.csswg.org/css-lists-3/#css-counter-reversed
group: counters
compat_features:
  - css.properties.counter-reset.reversed
