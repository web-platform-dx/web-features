name: <kbd>
description: The `<kbd>` element represents a span of inline text denoting textual user input and is usually rendered in a monospace font.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-kbd-element
group: html
compat_features:
  - html.elements.kbd
