name: alpha()
description: The `alpha()` CSS function computes a relative color value by adjusting the opacity of an origin color.
spec: https://drafts.csswg.org/css-color-5/#relative-alpha
group: color-types
compat_features:
  - css.types.color.alpha
