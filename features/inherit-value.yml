name: inherit
description: The `inherit` keyword can be applied to any CSS property, to 'inherit' the computed value of that property from the parent element.
spec: https://www.w3.org/TR/css-cascade/#inherit
caniuse: css-inherit-value
compat_features:
  - css.types.global_keywords.inherit
