name: Underline element
description: The `<u>` HTML element represents a span of inline text which should be rendered in a way that indicates that it has a non-textual annotation.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-u-element
group: html
compat_features:
  - html.elements.u
