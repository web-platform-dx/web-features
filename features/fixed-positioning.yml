name: Fixed positioning
description: "The `position: fixed` CSS declaration removes an element from the normal flow and positions an element relative to the viewport or page."
spec: https://drafts.csswg.org/css-position-3/#abspos-insets
group: layout
compat_features:
  - css.properties.position.fixed
