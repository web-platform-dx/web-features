name: prefers-reduced-data
description: The `prefers-reduced-data` CSS media query detects whether the user has a preference for using less internet traffic.
spec: https://drafts.csswg.org/mediaqueries-5/#prefers-reduced-data
group: media-queries
compat_features:
  - css.at-rules.media.prefers-reduced-data
