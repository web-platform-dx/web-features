name: <bdi>
description: The `<bdi>` element tells the browser's bidirectional algorithm to treat the text it contains in isolation from its surrounding text.
spec: https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-bdi-element
group: html
compat_features:
  - html.elements.bdi
