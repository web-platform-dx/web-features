name: import defer
description: The `import defer` JavaScript statement imports a module while deferring its evaluation until its first use.
spec: https://tc39.es/proposal-defer-import-eval/
group: js-modules
