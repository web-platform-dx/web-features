name: Deferred imports evaluation ("import defer")
description: "`import defer` allows importing JavaScript modules while deferring their evaluation to first usage, synchronously."
spec: https://tc39.es/proposal-defer-import-eval/
group: js-modules
