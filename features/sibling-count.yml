name: sibling-count() and sibling-index()
description: The `sibling-count()` and `sibling-index()` CSS functions TODO.
spec: TODO
compat_features:
  - css.types.sibling-count
  - css.types.sibling-index
group: css
