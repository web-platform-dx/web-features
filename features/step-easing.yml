name: steps() easing
description: The `steps()` easing function divides an animation or transition into equidistant steps. The `step-start` and `step-end` CSS properties are presets for specific scenarios.
spec: https://drafts.csswg.org/css-easing-2/#step-easing-functions
group: css
compat_features:
  - css.types.easing-function.steps
  - css.types.easing-function.steps.jump
