name: video-dynamic-range media feature
description: The `video-dynamic-range` CSS media feature sets styles based on whether a device can display video with at least `standard` range colors or at least `high` range colors. Non-visual devices will match neither.
spec: https://drafts.csswg.org/mediaqueries-5/#dynamic-range
group: media-queries
compat_features:
  - css.at-rules.media.video-dynamic-range
