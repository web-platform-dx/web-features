name: Scope pseudo
description: "The `:scope` CSS pseudo-class represents elements that are a reference point for selectors to match against."
spec: https://drafts.csswg.org/selectors-4/#the-scope-pseudo
group: selectors
compat_features:
  - css.selectors.scope
  - css.selectors.scope.dom_api
