name: createImageBitmap reqizeQuality
description: The `createImageBitmap()`'s' `resizeQuality` parameter allows specifying the algorithm to be used when resizing the input bitmap to match the output size.
spec: https://html.spec.whatwg.org/multipage/imagebitmap-and-animations.html#imagebitmap
compat_features:
  - api.createImageBitmap.options_resizeQuality_parameter
