name: Federated credential management
description: The `IdentityCredential` API delegates authentication to a third-party identity provider directly, instead of depending on third-party cookie. Also known as FedCM.
spec: https://fedidcg.github.io/FedCM/
group: credential-management
compat_features:
  - api.CredentialsContainer.get.identity_option
  - api.IdentityCredential
  - api.IdentityCredential.token
