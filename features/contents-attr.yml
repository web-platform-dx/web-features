name: "content: attr()"
description: "The `content: attr()` CSS declaration sets a `::before` or `::after` pseudo-element's content to the value of the specified HTML attribute."
spec: https://drafts.csswg.org/css-values-5/#attr-notation
group: css
compat_features:
  - css.types.attr
