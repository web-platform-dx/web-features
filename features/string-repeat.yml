name: String repeat()
description: The `repeat()` method of strings returns a new string consisting of the original string repeated `count` times.
spec: https://tc39.es/ecma262/multipage/text-processing.html#sec-string-objects
group: string
compat_features:
  - javascript.builtins.String.repeat
