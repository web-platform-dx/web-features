name: Gamut mapping
description: >
  Colors in CSS and `<canvas>` elements can define a color out of range of an output device.
  Gamut mapping brings the color into range while preserving hue and lightness.
spec: https://drafts.csswg.org/css-color-4/#css-gamut-mapping
