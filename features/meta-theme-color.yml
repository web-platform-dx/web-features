name: '<meta name="theme-color">'
description: The `name="theme-color"` attribute for the `<meta>` HTML element suggests a color that the browser may use to customize the display of the page or the browser user interface.
spec: https://html.spec.whatwg.org/multipage/semantics.html#meta-theme-color
group: html
caniuse: meta-theme-color
compat_features:
  - html.elements.meta.name.theme-color
