name: ::column
description: The `::column` CSS pseudo-element represents the individual columns in a multi-column container and can be styled with scroll-snap properties and have associated `::scroll-marker` pseudo-elements.
spec: https://drafts.csswg.org/css-multicol-2/#column-pseudo
group:
  - multi-column
  - scrolling
