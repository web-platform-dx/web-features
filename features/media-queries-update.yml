name: Update frequency media feature
description: The `update` CSS media feature sets styles based on whether and how fast the user's device can modify display after it has been rendered. Values are `none`, `slow`, and `fast`.
spec: https://drafts.csswg.org/mediaqueries-5/#update
group: media-queries
compat_features:
  - css.at-rules.media.update