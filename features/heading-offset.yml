name: Heading offset
description: The `headingoffset` attribute offsets the heading level of descendant `<h1>`–`<h6>` elements. The `headingreset` boolean attribute prevents heading offset computation from traversing beyond the element.
spec: https://html.spec.whatwg.org/multipage/sections.html#heading-levels-&-offsets
group: html
