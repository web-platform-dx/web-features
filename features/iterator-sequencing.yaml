name: Iterator.concat()
description: The `Iterator.concat(iterator, ...)` creates a sequenced iterator from a list of iterators.
spec: https://github.com/tc39/proposal-iterator-sequencing
group: iterators
compat_features:
  - javascript.builtins.Iterator.concat
