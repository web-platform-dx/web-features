name: if()
description: The if() function is an inline conditional value function that selects a value based on a set of conditions.
spec: https://drafts.csswg.org/css-values-5/#if-notation
group: css
# No compat keys yet, this feature is planned to ship with Chromium M136
