name: page-orientation
description: The `page-orientation` CSS property sets the rotation of a page after it has been layed out. This allows pages with the same `size` declaration to display at different orientations.
spec: https://drafts.csswg.org/css-page-3/#page-orientation-prop
group:
  - layout
  - print
compat_features:
  - css.at-rules.page.page-orientation
