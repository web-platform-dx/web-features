name: progress()
description: The `progress()` CSS function returns the relative position of one value between two other values. It can be used as an interpolation input to other calculations.
spec: https://drafts.csswg.org/css-values-5/#progress
group:
  - css
compat_features:
  - css.types.progress