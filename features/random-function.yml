name: random()
description: "The `random()` CSS function chooses enerates a random numeric value within a specified range. This allows for dynamic, randomized styling using CSS."
spec: https://drafts.csswg.org/css-values-5/#random
group: css
compat_features:
  - css.types.random
